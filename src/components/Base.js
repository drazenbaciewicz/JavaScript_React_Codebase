// This is a template for creating new reactJS files

// Importing React
import React from 'react';

export default class REPLACE_MY_NAME extends React.Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div>
                Hello, World!
            </div>
        )
    }
}